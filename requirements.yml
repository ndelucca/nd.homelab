---
# Ansible collection dependencies. Install with:
#   ansible-galaxy collection install -r requirements.yml
# Versions pinned to a major range for reproducible builds.
collections:
  - name: ansible.posix
    version: ">=2.2.0,<3.0.0"
  - name: community.general
    version: ">=12.6.1,<13.0.0"
