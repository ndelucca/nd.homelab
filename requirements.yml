---
# Dependencias de colecciones de Ansible. Instalar con:
#   ansible-galaxy collection install -r requirements.yml
# Versiones fijadas EXACTAS para builds reproducibles (las que están instaladas hoy).
# Para actualizar: subir el número acá y re-instalar con `ansible-galaxy collection install -r`.
collections:
  - name: ansible.posix
    version: "2.2.0"
  - name: community.general
    version: "12.6.1"
