---
# Cockpit-specific playbook
# Note: Run firewall role separately or use site.yml for complete setup
# Usage: ansible-playbook playbooks/cockpit.yml

- name: Install and configure Cockpit
  hosts: homeservers
  gather_facts: true

  roles:
    - cockpit
