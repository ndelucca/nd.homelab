---
# Backup-specific playbook (restic -> D-Ursa)
# Usage: ansible-playbook playbooks/backup.yml -l ndelucca-server

- name: Configure automated backups
  hosts: homeservers
  gather_facts: true

  roles:
    - backup
