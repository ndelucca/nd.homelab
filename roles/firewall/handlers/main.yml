---
# Handlers for Firewall role.
#
# No `reload firewalld` handler: every rule task uses `immediate: true` +
# `permanent: true`, so changes apply to the running firewall AND persist in one
# step. A reload would only re-read permanent config (redundant) and could drop
# runtime-only state, so it is intentionally omitted.
