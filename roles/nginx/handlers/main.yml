---
# Sin handlers: el reload de NGINX es inline en configure.yml (solo si el unit ya está activo)
# y el arranque final vive en el post_tasks de playbooks/site.yml (después de que AdGuard libera
# el puerto 53). El rol mainsail_tls (play de printers) trae su propio handler `Reload nginx`.
