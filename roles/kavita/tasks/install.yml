---
# Install Podman and dependencies (shared scaffolding in container_base).
# include_role also brings in the shared container handlers used below.

- name: Install the rootless Podman runtime and shared container handlers
  ansible.builtin.include_role:
    name: container_base
    tasks_from: install
