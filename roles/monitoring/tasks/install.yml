---
# Instala Podman y dependencias (scaffolding compartido en container_base).
# include_role además trae los handlers de container compartidos usados abajo.

- name: Install the rootless Podman runtime and shared container handlers
  ansible.builtin.include_role:
    name: container_base
    tasks_from: install
