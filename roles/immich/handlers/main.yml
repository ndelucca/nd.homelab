---
# Handlers for Immich are provided by the shared container_base role (pulled in
# from tasks/install.yml). This role only supplies the contract vars in
# defaults/main.yml (container_user/uid/service_name/selinux_paths) and notifies
# the generic handlers: `container daemon-reload-user`, `container restart`,
# `container apply selinux context`.
