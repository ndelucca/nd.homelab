---
jellyfin_enable_rpmfusion: true
jellyfin_rpmfusion_free_enabled: true
jellyfin_rpmfusion_nonfree_enabled: false

jellyfin_user: ndelucca
jellyfin_group: ndelucca

# El estado de la app vive en el disco de appdata (app_data_root, definido en
# group_vars/homeservers/storage.yml).
jellyfin_base_dir: "{{ app_data_root }}/jellyfin"
jellyfin_data_dir: "{{ jellyfin_base_dir }}/data"
jellyfin_config_dir: "{{ jellyfin_base_dir }}/config"
jellyfin_cache_dir: "{{ jellyfin_base_dir }}/cache"
jellyfin_log_dir: "{{ jellyfin_base_dir }}/log"

# Directorio de medios (películas, series, etc.). Apunta directo al media root
# compartido en el disco de datos, sin symlink. Las rutas de biblioteca de
# Jellyfin (.mblink) ya referencian estas rutas reales.
jellyfin_media_dir: "{{ app_media_root }}"

# Subdirectorios del media root que Jellyfin realmente sirve, y los ÚNICOS que
# etiqueta/relabelea (ver tasks/selinux.yml y el handler `Jellyfin apply selinux context`).
#
# NO usar jellyfin_media_dir acá. El media root es compartido: `Gallery` es de immich y
# `Books` de kavita, y esos son volumes de container que necesitan `container_file_t`.
# En file_contexts.local NO gana la regla más específica sino la ÚLTIMA que matchea (ver la
# nota de ordenamiento en CLAUDE.md), así que una regla sobre el root entero puede tapar las
# de esas subcarpetas. Peor todavía: el `restorecon -R` del handler repintaría esos volumes
# con lo que diga la DB, y si quedó tapada es public_content_rw_t → container_t no puede leer
# su propio volume → crash-loop y 502. Etiquetar sólo las bibliotecas propias saca a Jellyfin
# de esa carrera. Si Jellyfin sirve una biblioteca nueva, agregala explícitamente acá.
jellyfin_media_selinux_dirs:
  - "{{ app_media_root }}/Movies"
  - "{{ app_media_root }}/Series"

jellyfin_service_name: jellyfin
jellyfin_service_enabled: true
jellyfin_service_state: started

jellyfin_http_port: 8096
jellyfin_https_port: 8920

jellyfin_firewall_enabled: true

jellyfin_manage_selinux: true

jellyfin_enable_hardware_acceleration: false
