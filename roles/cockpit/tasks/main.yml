---
# Main tasks for Cockpit role

- name: Install Podman packages
  ansible.builtin.dnf:
    name: "{{ podman_packages }}"
    state: present
  become: true

- name: Install Cockpit packages
  ansible.builtin.dnf:
    name: "{{ cockpit_packages }}"
    state: present
  become: true
  notify: restart cockpit

- name: Create Cockpit configuration directory
  ansible.builtin.file:
    path: /etc/cockpit
    state: directory
    mode: '0755'
  become: true

- name: Deploy Cockpit reverse proxy configuration
  ansible.builtin.template:
    src: cockpit.conf.j2
    dest: /etc/cockpit/cockpit.conf
    owner: root
    group: root
    mode: '0644'
  become: true
  notify: restart cockpit socket

- name: Create cockpit.socket drop-in directory
  ansible.builtin.file:
    path: /etc/systemd/system/cockpit.socket.d
    state: directory
    mode: '0755'
  become: true

- name: Bind cockpit.socket to localhost (reachable only via NGINX)
  ansible.builtin.template:
    src: cockpit-socket-override.conf.j2
    dest: /etc/systemd/system/cockpit.socket.d/override.conf
    owner: root
    group: root
    mode: '0644'
  become: true
  notify: restart cockpit socket

- name: Enable and start Cockpit socket
  ansible.builtin.systemd:
    name: "{{ cockpit_service_name }}"
    enabled: "{{ cockpit_service_enabled }}"
    state: "{{ cockpit_service_state }}"
    daemon_reload: true
  become: true

- name: Enable and start Podman socket (for API access)
  ansible.builtin.systemd:
    name: podman.socket
    enabled: "{{ podman_enable_socket }}"
    state: "{{ 'started' if podman_enable_socket else 'stopped' }}"
    daemon_reload: true
  become: true

- name: Configure Podman for rootless operation
  ansible.builtin.command: loginctl enable-linger {{ ansible_user }}
  args:
    creates: /var/lib/systemd/linger/{{ ansible_user }}
  become: true

- name: Display Podman version
  ansible.builtin.command: podman --version
  register: podman_version_check
  changed_when: false
  check_mode: false  # read-only; run in --check for an accurate version display

- name: Show installed Podman version
  ansible.builtin.debug:
    msg: "Installed {{ podman_version_check.stdout }}"
